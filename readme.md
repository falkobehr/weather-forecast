# Weather Forecast Angular Module

## Grunt targets

```
    grunt d|d:dev               // Deploy prod|dev
    grunt w|w:scritps|w:styles  // Watch script & style development
    grunt v                     // Validate SASS & JavaScript files
    grunt t                     // Run Karma tests
```